"""Off-runtime experiments — code the serving API never imports.

Kept importable (tests, scripts, notebooks use them) but deliberately kept
OUT of backend/ so backend/ contains only code the live system runs.
"""
